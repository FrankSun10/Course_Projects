public class Handle {
	
	// because the priority queue is based on array, so the index of the array
	//   which is an integer should be enought to act as the reference
	
	int ref;
	
	public Handle(){
		ref = 0;
	}
}
